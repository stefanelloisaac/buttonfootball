/**
 * field.config — dimensões e geometria do campo (em unidades do mundo).
 * Origem (0,0) no canto superior esquerdo do campo.
 */

/** Largura total do campo (eixo X, gol a gol). */
export const FIELD_WIDTH = 1050

/** Altura total do campo (eixo Y, lateral a lateral). */
export const FIELD_HEIGHT = 680

/** Margem entre a borda do campo e o limite jogável. */
export const FIELD_MARGIN = 30

/** Abertura do gol (extensão no eixo Y). */
export const GOAL_WIDTH = 180

/** Profundidade do gol (quanto entra além da linha de fundo). */
export const GOAL_DEPTH = 40

/** Comprimento da grande área a partir da linha de fundo (eixo X). */
export const PENALTY_AREA_DEPTH = 165

/** Largura da grande área (eixo Y). */
export const PENALTY_AREA_WIDTH = 400

/** Raio do círculo central. */
export const CENTER_CIRCLE_RADIUS = 91.5
