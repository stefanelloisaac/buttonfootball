/**
 * core.math.util — funções matemáticas escalares puras.
 */

/** Restringe `value` ao intervalo [min, max]. */
export function clamp(value: number, min: number, max: number): number {
  // TODO: implementar clamp
  void value;
  void min;
  void max;
  throw new Error('NOT_IMPLEMENTED: clamp');
}

/** Interpolação linear entre `a` e `b` por `t` em [0, 1]. */
export function lerp(a: number, b: number, t: number): number {
  // TODO: implementar lerp
  void a;
  void b;
  void t;
  throw new Error('NOT_IMPLEMENTED: lerp');
}

/** Distância euclidiana entre dois pontos (componentes). */
export function distance(ax: number, ay: number, bx: number, by: number): number {
  // TODO: implementar distance
  void ax;
  void ay;
  void bx;
  void by;
  throw new Error('NOT_IMPLEMENTED: distance');
}

/** Número aleatório no intervalo [min, max). */
export function randRange(min: number, max: number): number {
  // TODO: implementar randRange
  void min;
  void max;
  throw new Error('NOT_IMPLEMENTED: randRange');
}
