/**
 * render.bodies.system — desenha botões (peças) e a bola.
 */
import type { GameState } from '../game';

/** Desenha todas as peças e a bola do estado atual. */
export function drawBodies(ctx: CanvasRenderingContext2D, state: GameState): void {
  // TODO: desenhar cada peça por time/goleiro e a bola; destacar peça da vez
  void ctx;
  void state;
}
