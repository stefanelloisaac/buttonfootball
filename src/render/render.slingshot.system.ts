/**
 * render.slingshot.system — desenha a linha de mira/força do estilingue.
 */
import type { DragState } from '../input';

/** Desenha a linha de mira do arraste atual (se houver). */
export function drawSlingshot(ctx: CanvasRenderingContext2D, drag: DragState | null): void {
  // TODO: linha do origin ao current, indicando direção e força
  void ctx;
  void drag;
}
