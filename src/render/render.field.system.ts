/**
 * render.field.system — desenha o campo, linhas, traves, áreas e círculo central.
 */

/** Desenha o campo completo no contexto 2D. */
export function drawField(ctx: CanvasRenderingContext2D): void {
  // TODO: grama, linhas de borda, meio-campo, círculo central, áreas, gols
  void ctx;
}
